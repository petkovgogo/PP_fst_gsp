#include "../inc/DataMapper.h"

int main()
{
    DataMapper dataMapper;

    dataMapper.showMap(); // outputs on different compilers, computers and operating systems are in the outputs folder
    
    return 0;
}